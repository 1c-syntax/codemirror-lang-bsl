/node_modules
/dist
/docs
/src/*.js
# Note: package-lock.json IS tracked so GHA's `cache: npm` works and builds
# are reproducible; src/*.d.ts files are intentionally tracked — they are
# hand-written type shims for the lezer-generated virtual modules
# (`./bsl.grammar`, `./bsl.grammar.terms`) that tsc cannot otherwise resolve.
